var React = require('react');

var SideBar = React.createClass({
    render: function () {
        return (
            <div>
                Sidebar
            </div>
        );
    }
});

module.exports = SideBar;